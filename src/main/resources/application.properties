spring.application.name=NexaX
server.port=${PORT:8080}

# === Railway MySQL Database (Public Network) ===
spring.datasource.url=jdbc:mysql://shuttle.proxy.rlwy.net:45296/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mOFkDOvQYGeJahVdakYLxnsYrRQaSFaC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Hibernate / JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === Payment Gateways (as is) ===
stripe.api.key = ${STRIPE_API_KEY}
razorpay.api.key = ${RAZORPAY_API_KEY}
razorpay.api.secret = ${RAZORPAY_API_SECRET}

